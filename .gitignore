# OS
.DS_Store

# Secrets
.env
.envrc
.cloudflare

# SSL certificates (private keys!)
.ssl/

# Project state (session-specific)
implementation-state.json

# Build artifacts (root — app/.gitignore covers app/*)
/.next/
/node_modules/

# Claude Code local state (per-machine)
.claude/

# Wrangler local state (per-machine)
.wrangler/

# Test artifacts
app/test-results/
/test-results/
app/reports/
app/.stryker-tmp/
.playwright-mcp/
*~
.mcp.json

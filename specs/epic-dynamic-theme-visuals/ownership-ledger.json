{
  "epic": "dynamic-theme-visuals",
  "concepts": [
    {
      "id": "dynamic-element-schema-shape",
      "name": "Dynamic-element / style-token schema shape (validation-only, never in client bundle)",
      "owning_module": "schema",
      "type": "public_contract",
      "added_by_story": "S1"
    },
    {
      "id": "dynamic-generators-registry-seam",
      "name": "DYNAMIC_GENERATORS name->function registry; watercolor(style, kind, render?) => self-contained SVG; forces size/format (never overridable by caller render knob); non-deterministic per call (unseeded); pinned style identity preserved",
      "owning_module": "dynamicVisuals",
      "type": "public_contract",
      "added_by_story": "S2"
    },
    {
      "id": "single-import-site-invariant",
      "name": "Randomize+override call shape / sole generator-import site (dynamicVisuals.ts is the only app/src module allowed a runtime import of the generator/stub)",
      "owning_module": "dynamicVisuals",
      "type": "invariant",
      "added_by_story": "S2",
      "review_note": "RESOLVED (S2->S3 gate-remediation); ACTIVATED (S7). The invariant is: dynamicVisuals.ts is the only app/src module that runtime-imports the ink GENERATOR. Phase A: bare '@ink/visuals' specifier, vacuously true (stub was inline). S7 retargeted the scanner's constant to the real '@rotheric/visuals' specifier (themes-validation.test.ts:1079) and its discriminating self-test (1152-1172) confirms it now genuinely flags the live import, not a vacuous placeholder. Regression-proof tests confirm S3/S5's legitimate DYNAMIC_GENERATORS import of dynamicVisuals.ts does NOT trip it."
    },
    {
      "id": "dynamic-banner-state-fail-soft",
      "name": "Dynamic banner state ownership; runtime generation failure is fail-soft (useThemeStyles.ts must NOT be modified)",
      "owning_module": "useDynamicBanner",
      "type": "invariant",
      "added_by_story": "S3",
      "review_note": "S3 verified: useThemeStyles.ts git-diff is empty (untouched); the hook's synchronous generator call is wrapped in try/catch that sets generatedSvg=null on any throw (AC-UX-3a, never uncaught). computeThemeStyles imported value-only (read-only), not mutated."
    },
    {
      "id": "resolve-dynamic-banner-style-seam",
      "name": "ResolveDynamicBannerStyleReturn { hasDynamicBanner, boxProps, style } \u2014 the two-field boxProps/style split (mirrors navBannerDecor; swapped backgroundImage stays on raw style, never folded into boxProps) + declaration-based hasDynamicBanner (reflects manifest, not per-attempt success) that S4's scrim keys off",
      "owning_module": "useDynamicBanner",
      "type": "public_contract",
      "added_by_story": "S3",
      "review_note": "Resolves the cross-doc discrepancy (VQ-S3-013): architecture.md's Seam Contract {boxProps, style} split wins over stories.json's flatter {hasDynamicBanner, backgroundImage:string}; the flat shape would have lost boxProps and invited folding backgroundImage into a single field (the Chakra silent-drop trap). hasDynamicBanner additively preserved from stories.json intent for S4. Split proven STRUCTURALLY by unit tests (boxProps has no backgroundImage key; style.backgroundImage carries the swap); real-browser render is MV-1 (waived Phase A, hard gate before S8). encodeSvgDataUri mirrors navBannerDecor byte-for-byte (verified)."
    },
    {
      "id": "worker-stateless-wrapper",
      "name": "banner.worker.ts holds no state of its own - pure message-passing wrapper around dynamicVisuals's generator call",
      "owning_module": "banner.worker",
      "type": "invariant",
      "added_by_story": "S5",
      "review_note": "S5 verified. banner.worker.ts is a thin wrapper: one self.addEventListener('message'), one DYNAMIC_GENERATORS.watercolor call in a try/catch, one postMessage per branch ({ok:true,svg} / {ok:false}); no blob math or randomizeParams duplication. It runtime-imports DYNAMIC_GENERATORS (a legitimate consumer) which does NOT trip S2's AC-STRUCT-4 scanner (that targets bare '@ink/visuals', confirmed - 98 themes-validation tests still green). Independent `make build`: the worker's response side-effect (postMessage({ok:...})) lands ONLY in its emitted chunk 6865, absent from _app; _app holds only the new Worker(new URL) call site + the generator (required by the main-thread fallback) + the message receiver. MV-3 build observation confirmed."
    },
    {
      "id": "scrim-logo-contrast-guarantee",
      "name": "Scrim presence/contrast guarantee - brand.500 logo text meets WCAG AA regardless of banner content",
      "owning_module": "Layout.tsx",
      "type": "invariant",
      "added_by_story": "S4",
      "review_note": "S4 verified. resolveScrimColor(brand500Hex) picks whichever of #000/#fff has the higher wcagRatio vs the ACTIVE theme's real brand.500 (activeThemeDefinition.colors.brand[5]; Scale10Schema tuple, SCALE_STEPS[5]=500, = Chakra's brand.500 token). The scrim is rendered OPAQUE, so it fully occludes the banner and the only contrast variable is scrim-vs-brand.500 (AC-A11Y-1 'regardless of banner content' by construction). Black-or-white guarantee is mathematically SOUND: both ratios <4.5 would require L<0.175 AND L>0.1833 (impossible); worst case at the crossover is ~4.58:1. Verified independently. contrast.ts untouched (git diff empty); wcagRatio reused read-only, order-independent (Boundary Rule 9). shouldRenderScrim is a declaration-based passthrough of S3's hasDynamicBanner (no flicker during the AC-UX-3a fallback window). No-scrim else-branch is byte-identical to the pre-existing logo JSX (no static-theme regression, AC-A11Y-2)."
    },
    {
      "id": "compute-theme-styles-purity",
      "name": "computeThemeStyles() purity - explicitly NOT touched by this epic; stays pure and keeps returning the static bannerDecorStyle",
      "owning_module": "useThemeStyles.ts",
      "type": "invariant",
      "added_by_story": null
    },
    {
      "id": "generator-catalog-validation-gate",
      "name": "Generator-catalog / style-token bounds validation gate (fail-loud at build time)",
      "owning_module": "themes-validation.test.ts",
      "type": "validation_rule",
      "added_by_story": null
    },
    {
      "id": "mocked-generator-no-per-seed-determinism",
      "name": "Mocked-generator behavioral test coverage convention - no per-seed determinism tests anywhere in the suite",
      "owning_module": "useDynamicBanner.test.ts",
      "type": "path_convention",
      "added_by_story": "S6",
      "review_note": "S6 enforces this via a hybrid AC-META-1 block: documentary convention + a live self-scan of the suite files for the snapshot-matcher family (markers built by concatenation so the scan doesn't trip on its own source - verified green) + a generator-signature check. Meaningful, not theater; the snapshot-matcher absence check catches the idiomatic determinism-test route. LOW: the signature check ('watercolor.length <= 3') is a loose proxy - it wouldn't catch a seed param added within arity 3 (current sig has only 2 required params), so its 'no seed-like parameter' claim is slightly overstated; the real defense is the snapshot scan + convention."
    }
  ],
  "stories": [
    {
      "story": "S1",
      "files": [
        "app/src/themes/schema.ts",
        "app/tests/unit/themes-validation.test.ts"
      ],
      "concepts_contributed": [
        "dynamic-element-schema-shape"
      ],
      "concepts_seeded_for_later": [
        "generator-catalog-validation-gate"
      ],
      "notes": "Added StyleToken/DynamicElement/DynamicTreatments schemas (all .strict()) + optional treatments.dynamic map to ThemeManifestSchema; generator enum declared inline per Boundary Rule 3. 21 new fail-asserting tests across 7 AC blocks. The generator-catalog-schema-sync test is a standalone placeholder pinning enum membership to ['watercolor']; TODO(S2) upgrades it to a real sync against DYNAMIC_GENERATORS keys once dynamicVisuals.ts exists."
    },
    {
      "story": "S2",
      "files": [
        "app/src/themes/treatments/dynamicVisuals.ts",
        "app/tests/unit/themes/treatments.test.ts",
        "app/tests/unit/themes-validation.test.ts"
      ],
      "concepts_contributed": [
        "dynamic-generators-registry-seam",
        "single-import-site-invariant"
      ],
      "concepts_realized_from_seed": [
        "generator-catalog-validation-gate"
      ],
      "notes": "Added dynamicVisuals.ts: DYNAMIC_GENERATORS registry with watercolor stub (mock MOCK-02-001). StyleToken derived by indexing ThemeManifest (no hand-redeclare, type-only schema import). Stub uses unseeded Math.random -> genuine per-call non-determinism (AC-UX-1); base fill derived only from pinned StyleToken; size/format forced to 420x96 and NOT overridable by caller render knob (verified by test). Output self-contained (no <script>/href/url) (AC-STRUCT-6). Upgraded S1's placeholder enum-sync test to pin schema enum against Object.keys(DYNAMIC_GENERATORS) - clean, no duplicate. AC-STRUCT-4 scanner reuses S1's parseImports/resolveSpecifierAbs (real resolved-specifier scan, not text grep) with a discriminating self-test block. All 114 tests green. OPEN ISSUE (escalated then RESOLVED via S2->S3 gate-remediation): the earlier AC-STRUCT-4 scanner target (dynamicVisuals.ts itself) forbade the runtime consumption S3/S5 require; now retargeted at the bare '@ink/visuals' specifier - see single-import-site-invariant.review_note."
    },
    {
      "story": "S3",
      "files": [
        "app/src/hooks/useDynamicBanner.ts",
        "app/tests/unit/hooks/useDynamicBanner.test.ts",
        "app/src/components/Layout.tsx"
      ],
      "concepts_contributed": [
        "dynamic-banner-state-fail-soft",
        "resolve-dynamic-banner-style-seam"
      ],
      "notes": "Added useDynamicBanner.ts: pure resolveDynamicBannerStyle(definition, generatedSvg) wrapped by a thin client-only useEffect/useState hook (Boundary Rule 7 - all branch logic in the pure fn). Layout.tsx calls it alongside the untouched useThemeStyles() and passes activeThemeDefinition from useAppTheme (= APP_THEMES[name], referentially stable, so useEffect[definition] only re-fires on real theme change - verified). Two-field boxProps/style split preserved end-to-end; backgroundImage lands on raw DOM style attr, never boxProps. encodeSvgDataUri mirrors navBannerDecor byte-for-byte. hasDynamicBanner is declaration-based (sound for S4 scrim-gating, no flicker during fallback window). 11 unit tests green, tsc --noEmit exit 0, useThemeStyles.ts diff empty. VERDICT: APPROVED. One LOW forward-looking note: Layout's transition:'background-image ...' is ineffective (background-image isn't transitionable) - harmless in Phase A (no dynamic theme, AC-PERF-1 scope is dimensions-only) but won't satisfy AC-PERF-1 'cross-fades in' at S8; recommend real opacity-layer cross-fade in S4/Phase B. MV-1 disclosure (acceptance-criteria.md) is honest and correctly waived for Phase A / re-raised as hard gate before S8."
    },
    {
      "story": "S4",
      "files": [
        "app/src/hooks/useDynamicBanner.ts",
        "app/src/components/Layout.tsx",
        "app/tests/unit/hooks/useDynamicBanner.test.ts"
      ],
      "concepts_contributed": [
        "scrim-logo-contrast-guarantee"
      ],
      "notes": "Added shouldRenderScrim(hasDynamicBanner) (declaration-based passthrough) + resolveScrimColor(brand500Hex) (opaque black/white, higher-wcagRatio pick) to useDynamicBanner.ts; Layout renders an opaque scrim Box behind the logo when a dynamic banner is declared. Resolved the brief's wrong single-app-wide-brand.500 assumption: brand.500 is per-theme, read from activeThemeDefinition.colors.brand[5] (verified = Chakra brand.500 token). Black-or-white >=4.5 guarantee mathematically verified sound (worst case ~4.58:1). contrast.ts untouched; wcagRatio read-only reuse (Boundary Rule 9). Tests: property-style over every real theme brand[5] + 12 arbitrary hexes using the REAL wcagRatio, signature-guard that resolveScrimColor takes no banner arg, and the fallback-window no-flicker check. 22/22 unit green, tsc exit 0. VERDICT: APPROVED. Two VERY-LOW optional notes (non-blocking): (a) the arbitrary-hex test comment says 'near the crossover band' but no value actually sits at L~0.179 - cosmetic, the math proof is unconditional; (b) resolveScrimColor inherits contrast.ts's hex-parseability assumption (a future named-CSS-color brand[5] would yield NaN -> pick white -> fail the A11Y test), consistent with the existing contrast gate - worth a one-line docstring caveat."
    },
    {
      "story": "S5",
      "files": [
        "app/src/workers/banner.worker.ts",
        "app/src/hooks/useDynamicBanner.ts",
        "app/tests/unit/hooks/useDynamicBanner.test.ts"
      ],
      "concepts_contributed": [
        "worker-stateless-wrapper"
      ],
      "notes": "Moved generation off the synchronous effect body (AC-PERF-2). Hook now tries `new Worker(new URL('../workers/banner.worker.ts', import.meta.url))` + postMessage/onmessage/onerror, falling back to scheduleFallbackGeneration (requestIdleCallback else setTimeout(0)) when Worker is undefined or construction throws. Pure S3/S4 functions (encodeSvgDataUri, resolveDynamicBannerStyle, shouldRenderScrim, resolveScrimColor) are BYTE-IDENTICAL (verified against prior reads); only new type imports + new helpers (resolveIdleScheduler, generateViaFallback, scheduleFallbackGeneration, resolveWorkerMessageOutcome) + the rewired useEffect were added. Race guard: per-effect-run `cancelled` closure + applyResult gate + worker?.terminate() on cleanup - structurally sound (MV-2). Fail-soft covers worker-internal throw ({ok:false}), worker.onerror, Worker-unavailable/constructor-throw (->fallback), and fallback generator throw (->null); no synchronous generator call on the effect body. 34 hook + 98 themes-validation = 132 unit green, tsc exit 0. Independent make build confirms worker side-effect isolated to chunk 6865, not _app (MV-3). VERDICT: APPROVED. Two LOW forward-looking notes: (a) the main-thread fallback necessarily bundles the generator into _app - trivial for the Phase-A inline stub, but at S7 AC-DEP-2's 'client bundle MUST NOT pull sharp/Node deps' must hold on the fallback path too, not just the worker; (b) theoretical benign double-apply if postMessage throws AFTER successful worker construction (both paths fail-soft, last-wins, no crash)."
    },
    {
      "story": "S6",
      "files": [
        "app/tests/unit/hooks/useDynamicBanner.test.ts"
      ],
      "concepts_contributed": [
        "mocked-generator-no-per-seed-determinism"
      ],
      "notes": "Test-only capstone (production files byte-identical to S5 - hook pure fns at identical line numbers, worker markers intact, tail identical; dynamicVisuals.ts/banner.worker.ts/Layout.tsx line counts match last-reviewed). Added AC-UX-4 (no-JS static fallback decoded against the RAW treatments.banner manifest field via an independently-authored encoding - genuinely non-circular w.r.t. computeThemeStyles, plus a multi-theme anti-coincidence check), AC-UX-5 (genuine decode: regex-extract payload + decodeURIComponent, then shape-check the DECODED value; round-trips a REAL DYNAMIC_GENERATORS.watercolor output exactly; no SVG-parser dep per Boundary Rule 8), AC-META-1 (hybrid negative assertion - see concept note). Full epic suite 162 green (45 hook + 19 treatments + 98 themes-validation), no orphaned TODO/FIXME/skip/only. Completeness sweep accurate: AC-A11Y-1/2 already covered by S4, AC-STRUCT-6 by S2's treatments.test.ts - nothing new needed. VERDICT: APPROVED. One LOW note on AC-META-1's signature-check looseness (non-blocking)."
    },
    {
      "story": "S7",
      "files": [
        "app/package.json",
        "app/package-lock.json",
        "app/src/themes/treatments/dynamicVisuals.ts",
        "app/tests/unit/themes/treatments.test.ts",
        "app/tests/unit/themes-validation.test.ts",
        "specs/epic-dynamic-theme-visuals/mocks-registry.json"
      ],
      "concepts_realized_from_seed": [
        "dynamic-generators-registry-seam",
        "single-import-site-invariant"
      ],
      "notes": "Swapped the Phase A inline stub for the real @rotheric/visuals package (git tag visuals-v0.1.2). Seam shape byte-identical (satisfies clause intact); S3/S5 consumers (useDynamicBanner.ts, banner.worker.ts, Layout.tsx, useThemeStyles.ts) have an empty git diff. Color identity flows correctly (StyleToken -> Params draw-time overrides, render assigned before style so render can never override the 4 pinned identity fields); forced envelope verified (viewBox 420x96, width/height override format); self-containment verified against real output (internal seed-namespaced filter refs only, no script/external href). AC-UX-1 rewritten from an exact-hsl-string match to a hue-tolerance check since the real package's HSLA output carries legitimate per-zone jitter the stub didn't have. MOCK-02-001 resolved (mocks-registry.json all_resolved: true). 117/117 re-targeted tests green against the real package; full epic suite 2061/2061 green; tsc clean. Gate-remediation round fixed 4 Stage-1 Opus findings: (1) sev-5 lockfile ssh/https reproducibility - resolved via documentation + empirical proof (cold-cache npm ci with GIT_SSH=/bin/false still installs via the ambient gh HTTPS+token credential; the ssh:// string in package-lock.json is npm's unavoidable hosted-git-info canonicalization, not a config bug); (2) sev-3 stale header docblock (render/style ordering); (3) sev-2 dead 'format: banner' literal (now documented as an intentional inert fallback); (4) sev-2 overclaimed render-safety comment (baseColor caveat added). Re-review after remediation: 0 findings, clean. Codex unavailable (out of credits, same as Phase A) - user re-authorized Opus-only review for this Phase B run too."
    },
    {
      "story": "S8",
      "files": [
        "app/src/themes/aquarelle/manifest.ts",
        "app/src/themes/registry.generated.ts",
        "app/tests/unit/themes-validation.test.ts",
        "app/tests/unit/themes/index.test.ts",
        "app/tests/unit/profile.themePicker.test.ts",
        "app/tests/e2e/banner-decor.spec.ts"
      ],
      "concepts_contributed": [],
      "notes": "New aquarelle light theme (anchorHue 250/analogous/55/45), genuinely additive: zero edits to schema.ts/dynamicVisuals.ts/useDynamicBanner.ts/Layout.tsx/useThemeStyles.ts (confirmed empty diffs). Frozen treatments.banner fallback confirmed structurally authentic (unmodified real @rotheric/visuals render at the pinned StyleToken - seed-namespaced filter IDs, feTurbulence/feDisplacementMap, hue clustering consistent with anchorHue 250). Static palette passes the real WCAG contrast gate with margin (all pairs >=6:1, independently recomputed). MV-1/MV-2 (previously waived in Phase A pending a theme that uses treatments.dynamic.banner) resolved genuinely in a real browser via 3 new e2e tests in banner-decor.spec.ts. Gate-remediation round fixed 3 Stage-1 Opus findings, all in the new e2e tests: (1) sev-5 MV-1's non-determinism assertion wasn't airtight against static-vs-dynamic swap timing - fixed with a waitForDynamicBannerSwap poll gate confirmed byte-for-byte against both production static-encode sites before comparing two guaranteed-dynamic reads; (2) sev-3 MV-2's blind waitForTimeout(500) settle window replaced with an expect.poll-based waitForBannerToSettle, per this project's own e2e_ac60 lesson against fixed waits; (3) sev-2 MV-2's console-error filter broadened to also catch generic React lifecycle warnings (unmounted/state update/setstate). Re-review after remediation: 0 findings, clean. Codex unavailable (same as S7); user's standing Opus-only authorization for this Phase B run covers this story too. SECOND remediation round (post-review, Decider-directed, 3 verification-examiner PARTIALs): visually inspected the frozen static banner and found it WAS a genuine outlier (washed-out/pale vs. a 20-sample distribution) - re-froze with a representative real sample (~54.7KB, still unmodified engine output); added an inline TODO(S9, AC-PERF-3) tracking comment noting the oversized static fallback awaits S9's lite-preset re-capture; waived the manual-THEME_IDS-registration finding as a pre-existing project-wide pattern every prior theme also required. All 13 banner-decor.spec.ts e2e tests (including the re-verified MV-1/MV-2) re-run green against the re-frozen banner; full 2065-test unit suite green."
    },
    {
      "story": "S9",
      "files": [
        "app/src/themes/aquarelle/manifest.ts",
        "app/src/themes/registry.generated.ts",
        "app/tests/unit/themes/treatments.test.ts"
      ],
      "concepts_contributed": [],
      "notes": "Capstone of Phase B - no new module. Finalized aquarelle's render lite-preset knobs {zones:2, layers:3, darkening:1, halo:0, splatter:0, smoothness:28}, resolving architecture.md's z.record Open Question. Discovered and pinned an undocumented @rotheric/visuals engine quirk: darkening<1 silently doubles a multiply-blend zone's path group (6 or 9 paths nondeterministically) - captured as a cross-project learning. Re-froze the static treatments.banner fallback at the finalized knobs (8567 chars/6 paths/0 circles, verified structurally consistent), superseding S8's oversized ~54.7KB capture and resolving its TODO(S9) comment. Flipped status experimental->stable on measured perf evidence: Node-level renderSVG timing (p50 0.11ms/p99 0.23ms), 4x CDP-throttled Chromium swap timing (generation completed off-thread, no main-thread jank observed), CLS 0.0046 (isolated to font-loading via a control-theme comparison, well below the 0.1 threshold). Honestly disclosed limitation: no real physical low-end device was reachable in this sandbox; the 4x throttle is documented as a proxy, not a real-device measurement. 3 new regression-guard unit tests against the real generator (exactly 6 paths, 0 circles, byte-band). Gate-remediation round fixed 2 Stage-1 Opus findings (both wording/comment-only, no logic change): (1) sev-3 a test title overclaimed capture provenance vs. what its assertions actually prove (structural consistency) - reworded; (2) sev-2 the darkening=1 guard was only incidentally covered by the path-count assertion with no comment naming it - added. Two other findings were informational/no-fix-needed (the stable-flip inference note, and a minor MIN_BYTES lower-bound looseness note). Re-review after remediation: 0 findings, clean. Codex unavailable (same as S7/S8); user's standing Opus-only authorization for this Phase B run covers this story too. Full 2068-test unit suite green. Verification-examiner: 12/13 YES; 1 PARTIAL (VQ-S9-012, CLS attribution used cross-theme not same-theme comparison) ACCEPTed by Decider on a ceiling argument (CLS is a sum of non-negative contributions, so the banner's share cannot exceed the measured total of 0.0046, ~20x under threshold; a same-theme A/B could only refine an already-passing number, not flip the verdict). Phase B (S7-S9) complete."
    }
  ],
  "reviewer_state": {
    "last_story_reviewed": "S9",
    "phase_b_status": "ALL STORIES S7-S9 REVIEWED & APPROVED (S7: 1 remediation round; S8: 1 gate-remediation round + 1 Decider-directed round after a genuine outlier static-banner capture was caught and re-frozen; S9: 1 remediation round, comment-only). Codex unavailable throughout Phase B (out of credits) - user authorized Opus-only review for the whole run. Ready for mutation/e2e/pre-commit gates.",
    "epic_phase_a_status": "ALL STORIES S1-S6 REVIEWED & APPROVED (S2 had 1 remediation round). Full Phase-A test suite internally consistent: 162 tests green across the 3 epic files, complete 1:1 AC-to-describe-block coverage, no orphaned placeholders/TODOs. Open items are all non-blocking forward-looking notes for Phase B/S7 (main-thread fallback bundles the generator -> AC-DEP-2 must hold on that path; S3 inert background-image cross-fade transition) + MV-1/2/3 browser-render manual checks waived for Phase A. Ready for mutation/e2e/pre-commit gates.",
    "s6_status": "APPROVED (162/162 epic-suite green, test-only confirmed, AC-UX-4 non-circular + AC-UX-5 genuine-decode verified; 1 LOW note on AC-META-1 signature check)",
    "s5_status": "APPROVED (132/132 unit green, tsc exit 0, pure S3/S4 fns byte-identical, worker chunk-isolation independently build-verified; 2 LOW forward-looking notes for S7)",
    "s2_status": "converged (AC-STRUCT-4 gate-remediation re-verified green, 117/117 in the two S2 files)",
    "s3_status": "APPROVED (11/11 unit green, tsc exit 0, useThemeStyles.ts untouched; 1 LOW non-blocking cross-fade note deferred to S4/Phase B)",
    "s4_status": "APPROVED (22/22 unit green, tsc exit 0, contrast.ts untouched, black/white contrast guarantee verified sound; 2 VERY-LOW optional notes)",
    "model_at": "2026-07-06T01:38:00Z",
    "precommit_gate_status": "WHOLE-TREE PASS COMPLETE (2026-07-07) - 2 findings, both severity < 5, non-blocking: (1) aquarelle manifest header regenerate-recipe duplicated the watercolor call shape [sev-3, doc drift risk] - FIXED (comment now points at dynamicVisuals.ts as sole source of truth instead of re-encoding the shape); (2) useDynamicBanner.ts:156/173 stale still-undesigned-aquarelle comments in already-committed Phase A code [sev-2] - left unfixed, out of this commit scope, disclosed as a Review note. All factual comment claims (8567 chars/6 paths/0 circles/3 filters), single-import-site invariant, per-theme folder convention, and registration-site consistency verified clean across the assembled S7-S9 tree. Cross-module reach-ins, contract/doc accuracy, cross-story duplication, convention-vs-structure: all clean. Codex unavailable throughout (out of credits); user-authorized Opus-only review for the entire Phase B run, including this final gate."
  }
}